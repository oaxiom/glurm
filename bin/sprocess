#!/usr/bin/env python3

import sys, os, glob, sqlite3

from glurm import db

if __name__ == '__main__':

    db = db(no_init=False) # Can init;

    # We are debugging, so reset on each sprocess for now:
    os.remove(os.path.expanduser('~/.glurm/database.db'))
    db.setup()

